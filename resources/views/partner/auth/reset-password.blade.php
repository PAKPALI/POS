@extends('layouts.public-auth')
@section('title', 'Nouveau mot de passe partenaire')
@section('content')
<div class="auth-flow auth-login-flow"><div class="auth-flow-heading"><span class="auth-flow-kicker"><i class="bi bi-key" aria-hidden="true"></i> Sécurité partenaire</span><h1>Choisissez un nouveau mot de passe.</h1><p>Utilisez un mot de passe unique et robuste.</p></div>@if($errors->any())<x-ui.alert variant="danger">{{ $errors->first() }}</x-ui.alert>@endif
<form method="POST" action="{{ route('partner.password.reset.update') }}">@csrf<input type="hidden" name="token" value="{{ $token }}"><x-ui.input id="email" name="email" type="email" label="Adresse e-mail" :value="$email ?? old('email')" required autocomplete="email" :error="$errors->first('email')" /><x-ui.password id="password" name="password" label="Nouveau mot de passe" hint="12 caractères minimum, avec majuscule, minuscule, chiffre et symbole." required autocomplete="new-password" :error="$errors->first('password')" /><x-ui.password id="password_confirmation" name="password_confirmation" label="Confirmer le mot de passe" required autocomplete="new-password" /><x-ui.form-actions class="auth-form-actions"><x-ui.button type="submit" class="w-100 auth-submit" loading-text="Modification…">Modifier le mot de passe</x-ui.button></x-ui.form-actions><p class="auth-flow-link"><a href="{{ route('partner.login') }}">Retour à la connexion</a></p></form></div>
@endsection
