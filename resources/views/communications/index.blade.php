@extends('layouts.layout')
@section('content')
<div class="container"><h1 class="page-header">Consommation SMS & WhatsApp</h1>
<div class="row g-3 mb-4"><div class="col-md-6"><div class="card"><div class="card-body"><div class="text-muted">SMS consommés</div><h2>{{ (int)($totals['sms'] ?? 0) }}</h2></div></div></div><div class="col-md-6"><div class="card"><div class="card-body"><div class="text-muted">WhatsApp consommés</div><h2>{{ (int)($totals['whatsapp'] ?? 0) }}</h2></div></div></div></div>
<form method="GET" class="card card-body mb-4"><div class="row g-3 align-items-end"><div class="col-md-2"><label class="form-label">Du</label><input type="date" name="from" value="{{ $filters['from'] ?? '' }}" class="form-control"></div><div class="col-md-2"><label class="form-label">Au</label><input type="date" name="to" value="{{ $filters['to'] ?? '' }}" class="form-control"></div><div class="col-md-2"><label class="form-label">Canal</label><select name="channel" class="form-select no-search"><option value="">Tous</option><option value="sms" @selected(($filters['channel']??'')==='sms')>SMS</option><option value="whatsapp" @selected(($filters['channel']??'')==='whatsapp')>WhatsApp</option></select></div><div class="col-md-2"><label class="form-label">Fonction</label><select name="function" class="form-select no-search"><option value="">Toutes</option><option value="sale" @selected(($filters['function']??'')==='sale')>Vente</option><option value="inventory" @selected(($filters['function']??'')==='inventory')>Inventaire</option><option value="invoice" @selected(($filters['function']??'')==='invoice')>Facture</option><option value="other" @selected(($filters['function']??'')==='other')>Autre</option></select></div><div class="col-md-2"><label class="form-label">Lignes par page</label><select name="per_page" class="form-select no-search">@foreach([10,25,50] as $size)<option value="{{ $size }}" @selected((int)($filters['per_page'] ?? 10)===$size)>{{ $size }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-theme w-100" type="submit" data-loading-text="Filtrage…">Filtrer</button></div></div></form>
<div class="card"><div class="table-responsive"><table class="table table-striped mb-0"><thead><tr><th>Date</th><th>Canal</th><th>Fonction</th><th>Pays</th><th>Destinataire</th><th>Unités</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->sent_at->format('d/m/Y H:i:s') }}</td><td>{{ strtoupper($log->channel) }}</td><td>{{ ['sale'=>'Vente','inventory'=>'Inventaire','invoice'=>'Facture','other'=>'Autre'][$log->function] ?? $log->function }}</td><td>{{ $log->country_code }}</td><td>{{ $log->recipient }}</td><td>{{ $log->units }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Aucun envoi pour ces filtres.</td></tr>@endforelse</tbody></table></div><div class="card-body d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3"><div class="small text-muted">Affichage de {{ $logs->firstItem() ?? 0 }} à {{ $logs->lastItem() ?? 0 }} sur {{ $logs->total() }} envoi(s)</div><div>{{ $logs->onEachSide(1)->links('pagination::bootstrap-5') }}</div></div></div></div>
@endsection
